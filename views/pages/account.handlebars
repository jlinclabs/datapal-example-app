
<div class="p-2">
  {{> avatar src=user.avatar alt=user.displayName}}
  <span>{{user.displayName}}</span>
  <a
    href="{{user.urls.user}}"
    class="link"
  >DataPal Account</a>
</div>

<div class="container px-4" style="">
  <pre><code>{{toJSON user}}</code></pre>
</div>
